# 更新日志

这里记录面向使用者的版本变化，版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)。
开发中的变更先记入「未发布」，发布前整理到 `## [版本号] - YYYY-MM-DD` 条目。
GitHub Release 从本文件提取对应版本的记录。

## [未发布]

### 新增

- 保存任务图后自动同步地图 GeoJSON：在同一工作区的 `map/<map_id>/geo_info/` 下找到 `source_task_id` 与当前任务一致的文件，按 `pose_context_key`（缺失时退回 `source_context_key`）把顶层位姿的底盘位置与姿态写入关联导航点。运行时以 GeoJSON 为准，示教后无需再手工修改地图文件。
- 同步只原地替换发生变化的数字，键顺序、缩进、整数写法和其他字段逐字节保留；结果接在保存提示之后，失败不影响已保存的任务图，再次保存即可重试。GeoJSON 需对当前登录用户可写。
- 新增应用图标：原创蜘蛛徽标，分深色版与浅色版。macOS 的 Dock、Windows 的标题栏和任务栏、Linux X11 的窗口图标随主题切换（石墨青、暮砂金用深色版，暖纸橙、雾白靛用浅色版）；macOS 应用包、Windows 可执行文件内置深色版静态图标，Linux 压缩包附带桌面入口和图标供 Wayland 使用。

### 优化

- 顶栏的「位姿取数」下拉菜单改回三个直接按钮：取底盘、取头部、取腰部，选中位姿后一次点击即可取数；默认窗口宽度下仍与「保存到远程」同处一行。

### 工程

- 更新 133 个依赖到最新兼容版本（ICU 2.3、内置 OpenSSL 3.6.3、libssh2-sys 0.3.3、ssh2 0.9.6 等），直接依赖均处于最新稳定大版本。
- Linux 发布包不再使用 UPX 压缩，三个平台的产物都是原始可执行文件；Linux 压缩包体积会相应变大。

## [0.11.0] - 2026-09-21

### 优化

- 重排日志分析页为「选择日志 / 分析设置 / 分析结果」三步卡片布局：文件列表行带图标与大小，底部行集中计数、选择与分页，模式改为分段控件，报告区新增统计卡、逐轮表斑马纹与状态徽章；分析口径与交互行为不变。
- SSH 连接模块移至顶栏：紧凑摘要条（状态点 + 连接目标 + 展开指示）点击展开或收起完整表单抽屉，未连接默认展开、连接成功自动收起、断开自动展开。
- 侧栏归入参数 / 流程图任务工作台（TaskWorkspace）内部、不再是全局骨架元素；日志分析页与工作台平级、独立全宽并自带页内文件列表；Ctrl/Cmd+S 保存快捷键全局可用（原先仅流程图视图生效）。
- 界面视觉现代化：控件行高 24→30，卡片圆角 12、控件圆角 8，边框柔化退后，字号与间距按 4 倍数网格重整，参数 / 流程图 / 日志分析三视图导航改为分段控件；色板与布局结构不变。

### 修复

- 浅色主题下，未挂在 Feathers 控件内的继承文本不再退化为默认白字。
- 截图验收夹具支持直接载入分析生成的 report.json（Result 信封格式）。

## [0.10.0] - 2026-09-17

### 新增

- 新增独立日志分析标签页，支持本地文件、目录选择及复用 SSH 连接只读浏览、下载远程日志，可取消分析并保留已生成产物。
- 内嵌日志分析核心，提供动作与轮次、工件节拍两种口径，支持暂停、错误与人工干预轮次纳入开关。
- 工件节拍按北京时间、进程边界及整数微秒计算，保留逐轮证据，明确扣除阻塞搬框等待，生成 JSON、统计报告和自包含交互 HTML。
- 报告直接展示逐轮原始耗时、扣除量、统计耗时与比例条，汇总有效样本、平均及极值；排除轮次保留原因和详情入口。

### 优化

- 远程日志仅显示有效日期目录及 master_control 文件，按日期和文件名创建时间倒序排列。
- 合并日志选择与统计设置，固定顶部报告目录和分析操作，分析完成后自动收起配置，报告导出文件按需展开。
- 精简全局界面：折叠连接高级设置，将位姿取数与流程结构操作收进菜单，移除重复保存按钮并缩窄侧栏。
- 报告统计与导出共用轮次判定和耗时计算；分页及界面设置变化不改变已生成报告的统计口径。

## [0.9.0] - 2026-09-08

### 新增

- 流程图支持完整属性编辑、节点新增/复制/删除/改名、连线新增/删除/重连、根入口与复合子图编辑，并提供原子撤销与重做。
- 依据机器人执行器的 44 种节点类型提供创建草稿与静态字段检查，保留未知字段、表达式及原文件格式；正确识别 parallel 的 branches 和条件叶/条件容器。
- 画布支持节点拖动、多选与框选、端口连线、鼠标锚点缩放、平移、适配画布与恢复自动布局；手工位置只保存在本地视图中。
- 连接区按钮常驻复用，连接、取消及断开时只更新显示和可用状态，并保持键盘焦点可用。
- 文件列表按连接来源和文件名复用已有行，刷新时仅增删和重排行，保留未变化的控件与滚动位置。
- 浏览和编辑模式均支持双击复合节点进入子图，单击仍为选中，拖动、多选及端口连线不会误触下钻。
- 图、context 和元数据共同显示未保存状态；切换文件、断开及关闭窗口前可继续编辑、保存后继续或放弃修改。保存期间的新修改不会被旧回执清除。

### 修复

- 修复输入 IP 后点击连接可能触发 `Entity despawned` 崩溃：通用按钮禁用态及插槽标记同步等待界面重建命令完成后执行。
- 补齐文件菜单、列表选择、参数树和流程属性插槽的重建顺序，避免换文档或新增连线时向旧控件提交延迟命令。
- 流程图对重复、保留或空白节点 ID 返回明确错误，避免布局崩溃；结构诊断显示原始 JSON 路径和原始/可绘制条数，异常子图保留诊断入口。
- 交叉边、平行边、自环及回边使用独立端口和通道；悬停追踪整条路径，点击查看起终点及原始连接数据，选边保留滚动位置。
- 创建 context 位姿不再重置流程图下钻位置；重载或清空文档仍回根，旧图层的点击不作用于新图层。
- 节点长参数可展开、收起和完整复制，支持查看原始节点 JSON 与非对象 inputs，保留焦点和详情滚动位置。
- 流程图出口不再额外空出一层。
- 编辑表单保留长文本、JSON 与非法草稿；应用属性不重建输入控件，旧文档和旧场景动作不会写入新文档。
- 加载或删除远端文件期间继续编辑，回执会保留新修改、非法输入与未完成粘贴；同帧保存意图不会转移到刚加载的其他文件，读取或解析失败也保留当前文档。
- 画布命中统一换算 DPI、界面缩放与图缩放，并阻止浮层下的意外选中、连线与滚动。
- 流程按钮的可用状态由对应业务单独维护，避免通用门控重新启用撤销、重做和重连按钮，以及连线重建时向旧实体发送命令。

### 工程

- 新增统一维护的更新日志，发布时自动将当前版本的变更显示到 GitHub Release 页面。

## [0.8.2] - 2026-09-08

### 修复

- 修复 `pick_poses` 等位姿数组元素无法选中的问题，选中后可获取并回填底盘位姿、头部关节和腰部关节。
- 回填绑定到发起请求时的具体数组元素，切换选中不会写错目标；重载文档后拒绝旧响应。
- 保留未编辑元素的原始 JSON、字符串形式、扩展字段及数值精度。
- 升级 `cfg_aliases` 至兼容补丁版本 0.2.2，消除 wgpu、naga、nix、winit 等依赖产生的 Rust 未来兼容性警告。

### 验证

- 211 项 Rust 测试、19 项部署与发布门禁回归通过。
- 真实任务文件的 10 个位姿通过实际 ROS2 底盘输出的内存回填与往返核验。
- Linux x64、macOS ARM64 和 Windows x64 构建通过。
