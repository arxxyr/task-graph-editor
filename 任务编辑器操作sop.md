# 任务图编辑器操作 SOP

按 **v0.8.2** 界面编写，安装包见 [最新发布](https://github.com/arxxyr/task-graph-editor/releases/latest)。

**操作顺序：启动 → 连接 → 加载并备份 → 编辑/取数 → 保存 → 重新加载复核。**

## 1. 启动

下载本机对应的包，解压后启动：

| 平台 | 安装包 | 启动方式 |
|---|---|---|
| Linux x64 | `linux-x64.tar.gz` | 运行 `./task-graph-editor` |
| macOS ARM64 | `macos-arm64.zip` | 双击「任务图编辑器.app」 |
| Windows x64 | `windows-x64.zip` | 双击 `task-graph-editor.exe` |

## 2. 连接并打开文件

1. 左侧填写**主机、端口、用户名、远程目录**，主机也可从下拉菜单选择。
2. 密码认证填写密码；密钥认证将密码留空，可填写私钥路径。远程目录支持 `~/Workspace/task_graphs`。
3. 点击「连接」，确认文件列表的「当前目录」正确。改目录后点击「刷新列表」。
4. 点击目标 JSON，切到「参数」，核对 `map_id`、`task_id` 和「保存位置」。
5. 右键目标文件选择「备份」，等左下角提示备份成功后再编辑。

## 3. 编辑参数或获取当前位姿

- 在「参数」中展开目标字段，修改位姿、数组或其他参数。位置按 **xyz**、姿态按 **wxyz** 核对。
- 需要从机器人取数时，先在连接后核对 **DOMAIN_ID** 与机器人一致，再点击目标位姿卡片使其高亮。
- 对 `pick_poses` 等数组：展开「其他」→ `pick_poses`，点击具体 **`[0]`、`[1]`…** 的卡片选中；只点数组总标题不能选中点位。
- 按需点击「获取底盘位姿」「获取头部关节」或「获取腰部关节」，等待完成后核对目标和数值。
  头部、腰部关节只回填对应“位置”栏的 **x / y**，不更新整个部位的位姿。

**取数只更新编辑区，仍需点击「应用到远程文件」才会保存。**

## 4. 保存并复核

1. 确认数值无误、输入框没有红色错误提示，点击「应用到远程文件」。
2. 等左下角提示「已更新远程文件」或「已更新并重命名」。
3. 再次点击保存后的文件，重新加载并核对修改项。未保存前重新加载会丢弃编辑。

保存后的文件名为 `{task_id}.json`，与原名不同时会重命名；同名目标已存在则拒绝保存。
保存位置以已加载文档为准，修改左侧目录不会改变当前文档的保存位置。

## 5. 常用功能与排查

- **上传**：点击左侧「上传文件」，或右键文件列表选择「上传文件」。上传到列表的当前目录，同名文件不会被覆盖。
- **流程图**：只读；点击节点查看详情，再次点击带子图的节点可进入子图。修改参数时切回「参数」。
- **主题与缩放**：右上角「选择主题」切换配色；Shift + 滚轮调整界面大小。
- **反馈问题**：点击左下角「复制提示」，复制完整错误信息。

| 问题 | 处理 |
|---|---|
| SSH 连接失败 | 核对网络、主机、端口、用户名及密码/私钥。 |
| 已连接但列表为空 | 核对远程目录、JSON 文件及读取权限，再点「刷新列表」。 |
| 取数按钮灰色 | 先加载文件，选中一个具体位姿卡片，并等待当前操作结束。 |
| 取数失败 | 核对 DOMAIN_ID，确认机器人 ROS2 正在运行；必要时请维护人员检查 `/tracked_pose`、`/joint_states`。 |
| 保存失败 | 修正红框中的非法数值，按提示检查写权限、同名文件或远端依赖。 |

保存时连接中断，或提示「文件已保存；警告」时，先刷新并核实文件状态，再决定是否重试。
