# 流程图编辑实施与验收

目标：完成研究方案 B、C、D，交付可保存、可撤销的节点属性与流程结构编辑，以及画布直接操作。
用户已确认按这三个阶段继续实施；沿用 `feat/graph-editing-research`，保留已完成的 A 阶段修复。

## 实施依据

- 编辑器入口：`model.rs::serialize_task_graph`、`ui/worker_bridge.rs::InputFlush` 与 `save_request`、
  `ui/graph_view.rs` 的场景版本、导航及独立详情插槽。
- 执行器源码：`/Users/loosqk/repo/cpp/ros2/master_control/src/master_control`。
  核对 `src/task_graph/node_factory.cpp`、容器执行代码与 `config/node_definitions_v2.json`，
  以实际加载/执行链路为准；定义表缺项不代表未知类型可以删除或拒绝。
- 图以完整 JSON 工作副本为编辑权威，`SubGraph` 为派生展示。内部稳定身份不写入机器人文件。
  加载时 `raw_json` 继续作为 context 无损合并基线，图编辑不得重造或覆盖未修改的 context。
- 复用既有 SSH 文档来源和原子保存。仅本地实现、测试和截图，不连接机器人、不发布。

## 文件与接口分工

| 负责人 | 范围 | 接口与职责 |
| --- | --- | --- |
| 模型 | `src/model.rs`、`src/model/graph_edit*` | `GraphDocument`、稳定 key、`GraphCommand`、事务/历史、图合并序列化 |
| 画布 | `src/ui/graph_view.rs`、`graph_view/canvas_edit*`、`graph_layout.rs` | viewport、拖动、端口、框选、缩放、导航恢复与画布刷新 |
| 整合 | `src/ui/graph_edit*`、`ui/mod.rs`、`worker_bridge*` | 编辑表单、同帧输入、应用命令、保存快照、未保存状态与文档生命周期 |
| 协议/验收 | 执行器只读核对、独立测试与文档 | 提取有依据的节点定义和执行规则、审计边界与完成证据 |

`TaskGraphData::apply_graph_command(expected_revision, command)` 校验后原子应用，
`undo_graph` / `redo_graph` 使用同一修订守卫。UI 意图另带 `document_version`，通过
`AppAction` 与 `InputFlush` 统一执行；旧实体、旧文档或旧修订动作不得被重新解释。

## 验收项

| 编号 | 必须完成的行为 | 验证方式 |
| --- | --- | --- |
| B1 | 原始/未知节点和边字段、缺省键、子图、字符串及数值类型无损保留；未改图不覆盖原图 | 模型往返测试 |
| B2 | 节点/边/子图稳定身份；插入、改名、撤销后不误定位；旧文档/修订拒绝 | 模型与 ECS 测试 |
| B3 | 属性编辑含字符串、布尔、数字、数组/对象；初始化全文，非法草稿明确提示，不静默保存旧值 | 表单输入与保存集成测试 |
| B4 | 一次命令一次事务，失败不改文档/历史；撤销/重做完整恢复，分叉新编辑清空重做 | 模型测试 |
| B5 | 图、context、元数据统一未保存状态；保存回执只确认提交快照，期间新编辑继续未保存，失败可重试 | 后台桥接测试 |
| B6 | 编辑→序列化→重载可见；输入焦点、选择与滚动在属性应用时保持；图撤销不撤销 context 或真实远端改名 | 集成与窗口核验 |
| B7 | 切换文件、断开或关闭前保护未保存内容与非法草稿；保存并继续只在提交成功且没有新修改时执行，过期确认失效 | 生命周期与排程测试 |
| C1 | 新增、删除、改名节点；删除节点及关联边一笔事务；复合子树完整撤销 | 模型与 UI 测试 |
| C2 | 新增、删除、重连边；平行边独立，标签等扩展保留；不把循环一律判错 | 模型与 UI 测试 |
| C3 | 创建、编辑、移除复合子图；跨层同名不误改；按执行器事实处理引用与分支语义 | 执行器依据、夹具和测试 |
| C4 | 节点定义以实际工厂/构造规则为据；未知类型保留原文；已知字段默认值和必填检查可追溯 | 定义核对与测试 |
| D1 | 节点/多节点拖动、阈值、取消和预览；边随位置更新；拖动不误下钻 | 几何、手势与窗口核验 |
| D2 | 端口拖拽创建边与重连，走相同事务；取消不改图 | ECS 与模型测试 |
| D3 | 独立缩放、鼠标锚点、平移、适配画布、恢复自动布局；手工位置仅为本地视图状态 | 几何与窗口核验 |
| D4 | 框选、多选和批量操作，跨层/重载清理；1×/2× DPI 与不同 UI 缩放命中一致 | 几何与 ECS 测试 |
| Q1 | A 阶段回归继续通过，fmt→clippy→test→build 和既有脚本检查通过 | 命令日志 |
| Q2 | 深/浅色与窄窗口可操作，新增表单和工具栏无溢出，实际渲染核验 | macOS 离线窗口截图 |

## 执行记录

- 已核对当前 A 阶段改动与分支状态，未提交改动完整保留。
- 执行器以 `af05e31` 为核对基线，实际工厂注册 44 种节点，静态 JSON 定义表仅有 31 种，
  因此采用工厂、构造器与执行路径作为权威。详细字段、引用、表达式及分支依据见
  [执行器协议](graph-executor-protocol.md)。
- B1/B2/B4：`src/model/graph_edit/tests.rs` 覆盖完整 JSON 保存重载、未知字段、缺省键、
  跨层同名、独立平行边、局部改名引用、事务失败回滚、历史分叉及稳定身份。
  类型切换按最终事务状态识别活跃子图，非活跃扩展保持原文。
- B3/B6：`src/ui/graph_edit/tests.rs` 通过真实 BSN 表单、`TextEdit`、`Activate` 和动作队列核验
  长文本、JSON、布尔、数值表达式、非法草稿、同帧输入后应用/保存，以及属性应用时的焦点、
  滚动和实体保留；旧文档、旧修订和跨帧旧面板事件均拒绝。
- B5/B7：`src/ui/document_state/tests.rs` 和图表单集成测试覆盖图/context/元数据脏状态、
  保存快照、保存期间的新修改、失败重试、重载与来源变化的旧回执、实际文件改名后的图撤销，
  以及切换/关闭提示、保存后继续、取消和过期放弃按钮。
  加载和删除当前来源另外记录发起快照及请求票据，回执到达时再次检查模型、草稿、非法数字文本、
  异步粘贴及同帧编辑意图，保留请求等待期间的新修改；读取或解析失败保留当前文档。
- C1/C2/C3：模型与真实按钮测试覆盖增删改名、克隆整个子树、独立边重连与删除、根入口设置，
  子图创建/清空/移除和撤销。条件叶/容器、parallel 的 branches 优先、被遮蔽 nodes 原文，
  以及条件入口改名后的原分支次序均有专门回归。
- C4：`src/model/graph_schema.rs` 的 11 项测试覆盖目录与静态约束；只读运行实际执行器的三份样例，
  共 1119 节点、251 层，全部通过解析与无损往返、属性保存重载、改名和撤销检查。
  90 个 parallel 改用 branches 的内存变体也通过，未回写原文件。
- D1/D2/D3/D4：`src/ui/graph_view/canvas_edit/tests.rs` 覆盖拖动阈值、预览与取消、框选、多节点移动，
  实际/虚拟端口正反向连线、重连身份、切层与重载、缩放/适配/自动布局及属性更新不重建。
  真实 UI 拾取验证 18 组 DPI/UiScale/图缩放组合；浮层覆盖时拦截新手势与滚轮，
  已捕获拖动保持连续，未保存确认层取消手势。`graph_layout/manual.rs` 检查移动后的正交避障与路径独立性。

## 最终整体验证

- 部署脚本、发布标签门禁和发布说明隔离回归：5 + 14 + 34，共 53 项通过。
- Q1：最终源码按顺序通过 `cargo fmt --all`、
  `cargo clippy --locked --all --all-targets -- -D warnings`、
  `CARGO_BUILD_WARNINGS=deny cargo test --locked --all --quiet`、
  `CARGO_BUILD_WARNINGS=deny cargo build --locked`。
  Rust 测试 381 项通过、0 失败，4 项需要外部文件的既有测试保持忽略；应用在 macOS ARM64 构建成功。
  无版本或依赖改动，未触发新增依赖的构建耗时报告要求。
- Q2：macOS 离线窗口已逐张检查 7 张有效画面：深色长文本与表单底部、浅色 960×600 模板区、
  条件边属性、非法草稿、未保存关闭提示及真实鼠标拖动/端口连线/缩放。错误详情已移到固定操作区下方，
  红框和原非法文本保留，长表单正文滚动不再隐藏错误。
  真实窗口输入确认节点移动 220 物理像素只改本地视图、端口连线新增独立平行边、Ctrl 滚轮缩放到 79%，
  模型只产生连线的一项事务。夹具和日志在 `target/graph-editing-visual/`，使用内存文档、禁用主题存储，
  不读取登录配置、不启动 SSH worker；未执行 Linux/Windows 实机交互或机器人运行。
- 连线重建曾暴露通用按钮门控与业务禁用态争写：新增 `ButtonGate::Managed` 指定唯一业务写入方，
  保留原 `Always` 语义。真实 BSN 测试枚举两种系统执行顺序、迟到按钮和资源变化，
  最终窗口重拍确认拖动/连线/缩放通过，相关四条旧实体命令警告全部消失。

B1～B7、C1～C4、D1～D4、Q1～Q2 的本地实施与上述验收已完成。

## 用户运行反馈补充：点击连接崩溃

首次验收的离线流程图窗口没有驱动真实连接状态转换，遗漏了连接面板按钮重建路径。
用户反馈输入 IP 后点击连接崩溃；正式 `RUST_BACKTRACE=1 cargo run --locked` 复现同一实体 `524v0`，
堆栈定位到 `widgets::sync_button_gates` 延迟插入 `InteractionDisabled`。
连接状态进入 `Connecting` 时，`rebuild_buttons` 已排队删除旧按钮，两者原先没有完成顺序依赖。

- 门控与插槽清理移到 `UiSet::Rebuild` 后，依赖自动 `ApplyDeferred` 先完成重建再查询；保持默认 panic 处理。
- 文件列表、菜单、参数树的同步明确依赖各自重建；流程属性面板等待父画布的 `GraphViewRebuilt`。
  未使用 `try_insert`、忽略命令错误或全局串行化掩盖问题。
- 新增 6 项回归：连接失败/取消状态转换、流程父子插槽替换、文件菜单、列表选择、旧位姿卡片及懒加载。
  均先确认旧顺序下 `Entity despawned` 可复现，再通过正式插件运行正向验证。
- 正式二进制的 macOS 原生窗口完成本机 IP 输入、连接失败、再次连接及取消验证；
  本机隔离监听服务确认客户端取消并关闭连接，窗口正常恢复，验证窗口随后正常关闭。
- 补充修复后的完整检查：fmt、Clippy 零警告，Rust 387 项通过、4 项既有测试忽略，应用构建通过。
- 按用户提供的实际地址，用修复后的正式二进制成功连接 `linux@192.168.5.155:22`，
  `/home/linux/Workspace/task_graphs` 正常列出 8 个文件。
  加载 `task_parallel_full_body_demo.json` 后，参数视图显示 10 个位姿点位，流程图显示全图 53 个节点、
  4 层、0 项诊断；进入 `all_body_parallel` 子图后正常显示 4 个节点和 8 条边，再切回参数视图正常。
  本次原生窗口验证未出现崩溃，运行日志为空；仅连接和读取，未修改远端文件或触发机器人动作。

## 控件复用与双击下钻

- 连接区固定生成连接、断开、刷新和上传四个按钮。状态变化仅修改显示、取消文案、禁用态和 Tab 顺序；
  隐藏或禁用焦点按钮时转移到可用入口，其他控件焦点保持。主动清除中断按压遗留的 `Pressed`，
  直接 `Activate` 也核对 Session，阻止隐藏按钮和同帧失效动作。
  生产插件回归覆盖失败、取消、成功、忙碌和重连，全程断言四个按钮及其文字子实体身份保持。
- 文件列表以连接代次、规范目录和文件名匹配行，仅增删和重排差异。新行先登记固定根实体再填充 BSN，
  空提示与右键空白区常驻；来源切换不复用同名文件。回归覆盖已有文字子实体与滚动保留、来源隔离，
  以及场景尚未落地时连续新增、删除和切换来源。
- 浏览和编辑模式都支持双击带子图的节点，详情入口保留。原生点击按稳定节点身份归并，因此文字与
  卡片空白之间也能双击；采用 Bevy 的多击间隔，导航回归夹具显式消除真实运行耗时依赖。
  拖动、修饰键、叶子和虚拟节点不下钻，旧场景输入失效；导航经过输入刷新队列并保护未提交草稿。
- 原生窗口连续双击另暴露“详情正在生成时父画布被删除”的竞态，会留下浮到窗口左上角的无父详情。
  父画布现等待后代插槽完成，并主动唤醒下一帧；旧图层详情和编辑表单不再排新批。
  真实 BSN 回归先在旧顺序下确认生成孤立根实体，再验证切层和重载后所有控件仍归属当前画布。
- 最终依次通过 fmt、Clippy 零警告、Rust 全量测试和构建：395 项通过，4 项既有外部文件测试忽略。
  macOS 原生窗口再次连接 `192.168.5.155`，正常刷新 8 个文件并保持选中，
  在 `task_parallel_full_body_demo.json` 中双击进入 `all_body_parallel`，再双击进入 `base_branch`，
  面包屑、画布和左栏正常，无悬空详情；最终运行日志 `target/reuse-regression/run-final.log` 为空。
  验证使用与构建产物逐字节一致的独立应用副本，未修改远端文件或执行机器人动作。

## 回退与交付边界

默认浏览模式保持可用，进入编辑流程后开放编辑工具。手工位置、缩放和内部 key 不进入机器人 JSON。
保存只经现有原子协议提交；本轮不自动提交、推送、部署或升级版本。
执行器仓库仅只读核对，结束时仍为干净工作区。应用改动保留在本研究分支供审阅，未提交或推送。
